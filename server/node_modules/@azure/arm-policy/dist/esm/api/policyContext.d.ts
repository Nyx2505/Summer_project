import type { AzureSupportedClouds } from "../static-helpers/cloudSettingHelpers.js";
import type { Client, ClientOptions } from "@azure-rest/core-client";
import type { TokenCredential } from "@azure/core-auth";
/** To manage and control access to your resources, you can define customized policies and assign them at a scope. */
export interface PolicyContext extends Client {
    /** The ID of the target subscription. The value must be an UUID. */
    subscriptionId: string;
    /** The API version to use for this operation. */
    /** Known values of {@link KnownVersions} that the service accepts. */
    apiVersion?: string;
}
/** Optional parameters for the client. */
export interface PolicyClientOptionalParams extends ClientOptions {
    /** The API version to use for this operation. */
    /** Known values of {@link KnownVersions} that the service accepts. */
    apiVersion?: string;
    /** Specifies the Azure cloud environment for the client. */
    cloudSetting?: AzureSupportedClouds;
}
/** To manage and control access to your resources, you can define customized policies and assign them at a scope. */
export declare function createPolicy(credential: TokenCredential, subscriptionId: string, options?: PolicyClientOptionalParams): PolicyContext;
//# sourceMappingURL=policyContext.d.ts.map