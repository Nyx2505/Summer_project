function serializeRecord(item, excludes, serializer) {
  excludes = excludes ?? [];
  const res = {};
  for (const key of Object.keys(item)) {
    if (excludes.includes(key) || item[key] === void 0) {
      continue;
    }
    if (serializer) {
      res[key] = serializer(item[key]);
    } else {
      res[key] = item[key];
    }
  }
  return res;
}
export {
  serializeRecord
};
