import { acquireAtManagementGroup, acquire } from "../../api/policyTokens/operations.js";
function _getPolicyTokens(context) {
  return {
    acquireAtManagementGroup: (managementGroupName, parameters, options) => acquireAtManagementGroup(context, managementGroupName, parameters, options),
    acquire: (parameters, options) => acquire(context, parameters, options)
  };
}
function _getPolicyTokensOperations(context) {
  return {
    ..._getPolicyTokens(context)
  };
}
export {
  _getPolicyTokensOperations
};
