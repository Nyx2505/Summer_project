var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var cancelScheduledNotification_exports = {};
__export(cancelScheduledNotification_exports, {
  cancelScheduledNotification: () => cancelScheduledNotification
});
module.exports = __toCommonJS(cancelScheduledNotification_exports);
var import_client = require("./internal/_client.js");
var import_tracing = require("../utils/tracing.js");
const OPERATION_NAME = "cancelScheduledNotification";
function cancelScheduledNotification(context, notificationId, options = {}) {
  return import_tracing.tracingClient.withSpan(
    `NotificationHubsClientContext.${OPERATION_NAME}`,
    options,
    async (updatedOptions) => {
      const endpoint = context.requestUrl();
      endpoint.pathname += `/schedulednotifications/${notificationId}`;
      const headers = await context.createHeaders(OPERATION_NAME);
      const request = (0, import_client.createRequest)(endpoint, "DELETE", headers, updatedOptions);
      const response = await (0, import_client.sendRequest)(context, request, 200);
      return (0, import_client.parseNotificationSendResponse)(response);
    }
  );
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  cancelScheduledNotification
});
//# sourceMappingURL=cancelScheduledNotification.js.map
