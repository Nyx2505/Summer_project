var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getNotificationHubJob_exports = {};
__export(getNotificationHubJob_exports, {
  getNotificationHubJob: () => getNotificationHubJob
});
module.exports = __toCommonJS(getNotificationHubJob_exports);
var import_client = require("./internal/_client.js");
var import_notificationHubJobSerializer = require("../serializers/notificationHubJobSerializer.js");
var import_tracing = require("../utils/tracing.js");
const OPERATION_NAME = "getNotificationHubJob";
function getNotificationHubJob(context, jobId, options = {}) {
  return import_tracing.tracingClient.withSpan(
    `NotificationHubsClientContext.${OPERATION_NAME}`,
    options,
    async (updatedOptions) => {
      const endpoint = context.requestUrl();
      endpoint.pathname += `/jobs/${jobId}`;
      const headers = await context.createHeaders(OPERATION_NAME);
      headers.set("Content-Type", "application/atom+xml;type=entry;charset=utf-8");
      const request = (0, import_client.createRequest)(endpoint, "GET", headers, updatedOptions);
      const response = await (0, import_client.sendRequest)(context, request, 200);
      return (0, import_notificationHubJobSerializer.parseNotificationHubJobEntry)(response.bodyAsText);
    }
  );
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getNotificationHubJob
});
//# sourceMappingURL=getNotificationHubJob.js.map
