var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var listRegistrationsByChannel_exports = {};
__export(listRegistrationsByChannel_exports, {
  listRegistrationsByChannel: () => listRegistrationsByChannel
});
module.exports = __toCommonJS(listRegistrationsByChannel_exports);
var import_listRegistrations = require("./internal/_listRegistrations.js");
var import_registrationUtils = require("../utils/registrationUtils.js");
var import_tracing = require("../utils/tracing.js");
function listRegistrationsByChannel(context, channel, options = {}) {
  const newOptions = {
    ...options,
    filter: (0, import_registrationUtils.getFilterByChannel)(channel)
  };
  const { span, updatedOptions } = import_tracing.tracingClient.startSpan(
    "NotificationHubsClientContext.listRegistrationsByDevice",
    newOptions
  );
  try {
    const iter = (0, import_listRegistrations.listRegistrationsAll)(context, updatedOptions);
    return {
      next() {
        return iter.next();
      },
      [Symbol.asyncIterator]() {
        return this;
      },
      byPage: () => {
        return (0, import_listRegistrations.listRegistrationPagingPage)(context, updatedOptions);
      }
    };
  } catch (e) {
    span.setStatus({ status: "error", error: e });
    throw e;
  } finally {
    span.end();
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  listRegistrationsByChannel
});
//# sourceMappingURL=listRegistrationsByChannel.js.map
