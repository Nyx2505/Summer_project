var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var createOrUpdateRegistrationDescription_exports = {};
__export(createOrUpdateRegistrationDescription_exports, {
  createOrUpdateRegistrationDescription: () => createOrUpdateRegistrationDescription
});
module.exports = __toCommonJS(createOrUpdateRegistrationDescription_exports);
var import_client = require("./_client.js");
var import_registrationSerializer = require("../../serializers/registrationSerializer.js");
var import_utils = require("../../utils/utils.js");
async function createOrUpdateRegistrationDescription(context, registration, operationName, options) {
  const endpoint = context.requestUrl();
  endpoint.pathname += "/registrations";
  let httpMethod = "POST";
  if (operationName === "createOrUpdate" || operationName === "update") {
    endpoint.pathname += `/${registration.registrationId}`;
    httpMethod = "PUT";
  }
  const etag = registration.etag;
  registration.registrationId = void 0;
  registration.etag = void 0;
  const headers = await context.createHeaders(`${operationName}Registration`);
  headers.set("Content-Type", "application/atom+xml;type=entry;charset=utf-8");
  if (operationName === "update") {
    headers.set("If-Match", (0, import_utils.isDefined)(etag) ? `"${etag}"` : "*");
  }
  const request = (0, import_client.createRequest)(endpoint, httpMethod, headers, options);
  request.body = import_registrationSerializer.registrationDescriptionSerializer.serializeRegistrationDescription(registration);
  const response = await (0, import_client.sendRequest)(context, request, [200, 201]);
  return import_registrationSerializer.registrationDescriptionParser.parseRegistrationEntry(response.bodyAsText);
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  createOrUpdateRegistrationDescription
});
//# sourceMappingURL=_createOrUpdateRegistrationDescription.js.map
