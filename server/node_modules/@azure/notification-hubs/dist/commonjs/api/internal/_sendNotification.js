var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var sendNotification_exports = {};
__export(sendNotification_exports, {
  sendNotificationInternal: () => sendNotificationInternal
});
module.exports = __toCommonJS(sendNotification_exports);
var import_client = require("./_client.js");
var import_optionUtils = require("../../utils/optionUtils.js");
var import_notificationUtils = require("../../utils/notificationUtils.js");
var import_core_util = require("@azure/core-util");
var import_tracing = require("../../utils/tracing.js");
function sendNotificationInternal(context, notification, options) {
  return import_tracing.tracingClient.withSpan(
    `NotificationHubsClientContext.sendNotification`,
    options,
    async (updatedOptions) => {
      const endpoint = context.requestUrl();
      endpoint.pathname += "/messages/";
      if ((0, import_optionUtils.isDirectSendNotificationOptions)(options) && Array.isArray(options.deviceHandle)) {
        endpoint.pathname += "$batch";
      }
      if (((0, import_optionUtils.isSendNotificationOptions)(options) || (0, import_optionUtils.isBroadcastSendNotificationOptions)(options)) && options.enableTestSend) {
        endpoint.searchParams.append("test", "true");
      }
      const headers = await context.createHeaders(
        "sendNotification",
        notification.headers
      );
      headers.set("ServiceBusNotification-Format", notification.platform);
      let body = notification.body;
      let contentType = notification.contentType;
      if ((0, import_optionUtils.isDirectSendNotificationOptions)(options) && Array.isArray(options.deviceHandle)) {
        endpoint.searchParams.append("direct", "true");
        const boundary = `nh-boundary-${(0, import_core_util.randomUUID)()}`;
        contentType = `multipart/mixed; boundary = "${boundary}"`;
        body = (0, import_notificationUtils.createMultipartDirectNotification)(boundary, notification, options.deviceHandle);
      } else if ((0, import_optionUtils.isDirectSendNotificationOptions)(options)) {
        endpoint.searchParams.append("direct", "true");
        if (notification.platform === "browser") {
          const browserHandle = options.deviceHandle;
          headers.set("ServiceBusNotification-DeviceHandle", browserHandle.endpoint);
          headers.set("Auth", browserHandle.auth);
          headers.set("P256DH", browserHandle.p256dh);
        } else {
          headers.set("ServiceBusNotification-DeviceHandle", options.deviceHandle);
        }
      } else if ((0, import_optionUtils.isSendNotificationOptions)(options)) {
        headers.set("ServiceBusNotification-Tags", options.tagExpression);
      }
      headers.set("Content-Type", contentType);
      headers.set("ServiceBusNotification-Format", notification.platform);
      const request = (0, import_client.createRequest)(endpoint, "POST", headers, updatedOptions);
      request.body = body;
      const response = await (0, import_client.sendRequest)(context, request, 201);
      return (0, import_client.parseNotificationSendResponse)(response);
    }
  );
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  sendNotificationInternal
});
//# sourceMappingURL=_sendNotification.js.map
