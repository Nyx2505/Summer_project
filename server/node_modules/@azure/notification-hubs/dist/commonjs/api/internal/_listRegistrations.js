var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var listRegistrations_exports = {};
__export(listRegistrations_exports, {
  listRegistrationPagingPage: () => listRegistrationPagingPage,
  listRegistrationsAll: () => listRegistrationsAll
});
module.exports = __toCommonJS(listRegistrations_exports);
var import_client = require("./_client.js");
var import_registrationSerializer = require("../../serializers/registrationSerializer.js");
async function* listRegistrationsAll(context, options) {
  for await (const page of listRegistrationPagingPage(context, options)) {
    yield* page;
  }
}
async function* listRegistrationPagingPage(context, options) {
  let result = await _listRegistrations(context, options);
  yield result.registrations || [];
  let continuationToken = result.continuationToken;
  while (continuationToken) {
    result = await _listRegistrations(context, options, continuationToken);
    continuationToken = result.continuationToken;
    yield result.registrations || [];
  }
}
async function _listRegistrations(context, options, continuationToken) {
  const endpoint = context.requestUrl();
  endpoint.pathname += "/registrations";
  if (options.top !== void 0) {
    endpoint.searchParams.set("$top", `${options.top}`);
  }
  if (options.filter !== void 0) {
    endpoint.searchParams.set("$filter", options.filter);
  }
  if (continuationToken !== void 0) {
    endpoint.searchParams.set("continuationtoken", continuationToken);
  }
  const headers = await context.createHeaders("listRegistrations");
  const request = (0, import_client.createRequest)(endpoint, "GET", headers, options);
  const response = await (0, import_client.sendRequest)(context, request, 200);
  const registrations = await import_registrationSerializer.registrationDescriptionParser.parseRegistrationFeed(
    response.bodyAsText
  );
  const nextToken = response.headers.get("x-ms-continuationtoken");
  return {
    registrations,
    continuationToken: nextToken
  };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  listRegistrationPagingPage,
  listRegistrationsAll
});
//# sourceMappingURL=_listRegistrations.js.map
