var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var submitNotificationHubJob_exports = {};
__export(submitNotificationHubJob_exports, {
  submitNotificationHubJob: () => submitNotificationHubJob
});
module.exports = __toCommonJS(submitNotificationHubJob_exports);
var import_client = require("./internal/_client.js");
var import_notificationHubJobSerializer = require("../serializers/notificationHubJobSerializer.js");
var import_tracing = require("../utils/tracing.js");
const OPERATION_NAME = "submitNotificationHubJob";
function submitNotificationHubJob(context, job, options = {}) {
  return import_tracing.tracingClient.withSpan(
    `NotificationHubsClientContext.${OPERATION_NAME}`,
    options,
    async (updatedOptions) => {
      const endpoint = context.requestUrl();
      endpoint.pathname += "/jobs";
      const headers = await context.createHeaders(OPERATION_NAME);
      headers.set("Content-Type", "application/atom+xml;type=entry;charset=utf-8");
      const request = (0, import_client.createRequest)(endpoint, "POST", headers, updatedOptions);
      request.body = (0, import_notificationHubJobSerializer.serializeNotificationHubJobEntry)(job);
      const response = await (0, import_client.sendRequest)(context, request, 201);
      return (0, import_notificationHubJobSerializer.parseNotificationHubJobEntry)(response.bodyAsText);
    }
  );
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  submitNotificationHubJob
});
//# sourceMappingURL=submitNotificationHubJob.js.map
