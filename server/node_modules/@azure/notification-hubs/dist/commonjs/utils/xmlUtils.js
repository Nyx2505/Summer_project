var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var xmlUtils_exports = {};
__export(xmlUtils_exports, {
  XML_METADATA_MARKER: () => XML_METADATA_MARKER,
  XML_VALUE_MARKER: () => XML_VALUE_MARKER,
  isJSONLikeObject: () => isJSONLikeObject,
  parseXMLError: () => parseXMLError,
  sanitizeSerializableObject: () => sanitizeSerializableObject,
  serializeToAtomXmlRequest: () => serializeToAtomXmlRequest
});
module.exports = __toCommonJS(xmlUtils_exports);
var import_utils = require("./utils.js");
var import_core_xml = require("@azure/core-xml");
const XML_METADATA_MARKER = "$";
const XML_VALUE_MARKER = "_";
const EMPTY_JSON_OBJECT_CONSTRUCTOR = {}.constructor;
function isJSONLikeObject(value) {
  return typeof value === "object" && value.constructor === EMPTY_JSON_OBJECT_CONSTRUCTOR;
}
function sanitizeSerializableObject(resource) {
  Object.keys(resource).forEach(function(property) {
    if (!(0, import_utils.isDefined)(resource[property])) {
      delete resource[property];
    } else if (isJSONLikeObject(resource[property])) {
      sanitizeSerializableObject(resource[property]);
    }
  });
}
function serializeToAtomXmlRequest(resourceName, resource) {
  const content = {};
  content[resourceName] = Object.assign({}, resource);
  sanitizeSerializableObject(content[resourceName]);
  content[resourceName][XML_METADATA_MARKER] = {
    xmlns: "http://schemas.microsoft.com/netservices/2010/10/servicebus/connect",
    "xmlns:i": "http://www.w3.org/2001/XMLSchema-instance"
  };
  content[XML_METADATA_MARKER] = { type: "application/xml" };
  const requestDetails = {
    updated: (/* @__PURE__ */ new Date()).toISOString(),
    content
  };
  requestDetails[XML_METADATA_MARKER] = {
    xmlns: "http://www.w3.org/2005/Atom"
  };
  return requestDetails;
}
async function parseXMLError(bodyText) {
  if (!bodyText) {
    return;
  }
  let result;
  try {
    const xmlError = await (0, import_core_xml.parseXML)(bodyText, { includeRoot: true });
    if (Object.hasOwnProperty.call(xmlError, "Error") && Object.hasOwnProperty.call(xmlError["Error"], "Detail")) {
      return xmlError["Error"]["Detail"];
    }
  } catch {
  }
  return result;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  XML_METADATA_MARKER,
  XML_VALUE_MARKER,
  isJSONLikeObject,
  parseXMLError,
  sanitizeSerializableObject,
  serializeToAtomXmlRequest
});
//# sourceMappingURL=xmlUtils.js.map
