var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var utils_exports = {};
__export(utils_exports, {
  getDate: () => getDate,
  getDateOrUndefined: () => getDateOrUndefined,
  getFloat: () => getFloat,
  getFloatOrUndefined: () => getFloatOrUndefined,
  getInteger: () => getInteger,
  getIntegerOrUndefined: () => getIntegerOrUndefined,
  getString: () => getString,
  getStringOrUndefined: () => getStringOrUndefined,
  getTagsOrUndefined: () => getTagsOrUndefined,
  isDefined: () => import_core_util.isDefined,
  isString: () => isString
});
module.exports = __toCommonJS(utils_exports);
var import_core_util = require("@azure/core-util");
function isString(thing) {
  return typeof thing === "string" || thing instanceof String;
}
function getString(value, nameOfProperty) {
  const result = getStringOrUndefined(value);
  if (result === void 0) {
    throw new Error(
      `"${nameOfProperty}" received from service expected to be a string value and not undefined.`
    );
  }
  return result;
}
function getStringOrUndefined(value) {
  if (!(0, import_core_util.isDefined)(value)) {
    return void 0;
  }
  return value.toString();
}
function getInteger(value, nameOfProperty) {
  const result = getIntegerOrUndefined(value);
  if (result === void 0) {
    throw new Error(
      `"${nameOfProperty}" received from service expected to be a number value and not undefined.`
    );
  }
  return result;
}
function getIntegerOrUndefined(value) {
  if (!(0, import_core_util.isDefined)(value)) {
    return void 0;
  }
  const result = parseInt(value.toString());
  return isNaN(result) ? void 0 : result;
}
function getFloat(value, nameOfProperty) {
  const result = getFloatOrUndefined(value);
  if (result === void 0) {
    throw new Error(
      `"${nameOfProperty}" received from service expected to be a number value and not undefined.`
    );
  }
  return result;
}
function getFloatOrUndefined(value) {
  if (!(0, import_core_util.isDefined)(value)) {
    return void 0;
  }
  const result = parseFloat(value.toString());
  return Number.isNaN(result) ? void 0 : result;
}
function getDate(value, nameOfProperty) {
  const result = getDateOrUndefined(value);
  if (result === void 0) {
    throw new Error(
      `"${nameOfProperty}" received from service expected to be a Date value and not undefined.`
    );
  }
  return result;
}
function getDateOrUndefined(value) {
  const stringValue = getStringOrUndefined(value);
  if (stringValue === void 0) {
    return void 0;
  }
  const result = new Date(stringValue.toString());
  return Number.isNaN(+result) ? void 0 : result;
}
function getTagsOrUndefined(value) {
  const result = getStringOrUndefined(value);
  if (result === void 0) {
    return void 0;
  }
  return result.split(",");
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getDate,
  getDateOrUndefined,
  getFloat,
  getFloatOrUndefined,
  getInteger,
  getIntegerOrUndefined,
  getString,
  getStringOrUndefined,
  getTagsOrUndefined,
  isDefined,
  isString
});
//# sourceMappingURL=utils.js.map
